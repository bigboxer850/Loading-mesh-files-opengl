Instructions to run the file

This codes load a mesh file. mesh file is generally a file contaning the file with point coordiates and there connection.It should be in form of triangles i.e the which points are connected to form traingles should be given.

 

